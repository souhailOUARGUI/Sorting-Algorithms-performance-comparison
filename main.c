#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <math.h>
#include <string.h>
#include "Functions/Proj.h" // fonctions et Algorithmes
#include "Functions/gnuConnect.h" // fonctions et Algorithmes


 
int main()
{
   
    // int *list = GenerateRandomList(15000);
    // int size = 15000;

    // EclapesedTime(Tri_bulle, list, size);
    // EclapesedTime(Tri_Selection, list, size);
    // EclapesedTime(Tri_Insertion, list, size);
    // EclapesedTime(shellSort, list, size);

    // gnuConnect(Tri_bulle, list, size);
    // gnuConnect();




///////////////////////////////:

    calculate_print();
    Plotgnu();

///////////////////////////////:

    // printf("original list : ");
    // print_list(list, size);
    return 0;
}